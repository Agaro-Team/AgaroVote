type VotingPool @entity(immutable: true) {
  id: ID!
  version: BigInt!
  poolHash: Bytes!
}

type Vote @entity(immutable: true) {
  id: ID!
  poolHash: Bytes!
  voter: Bytes!
  candidateSelected: BigInt!
  newPoolVoterHash: Bytes!
  timestamp: BigInt!
}
